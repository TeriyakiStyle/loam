// ---------------------------------------------------------------------------
// CONTACT — placeholder.
//
// Deliberately empty of real details: put your own address or form in here.
// ---------------------------------------------------------------------------

export function render(el, _store) {
  el.innerHTML = `
    <section class="page">
      <h1>Contact</h1>
      <p class="placeholder">Contact details go here.</p>
    </section>
  `;
  return () => {};
}
